# CryptoCore - Криптографическая утилита командной строки

**CryptoCore** — учебный проект, реализующий полный набор криптографических операций для защиты данных. Разработан на языке C с использованием OpenSSL для криптографических примитивов.

![Build Status](https://img.shields.io/badge/build-passing-success)
![Tests](https://img.shields.io/badge/tests-passing-success)
![License](https://img.shields.io/badge/license-MIT-blue)
![Language](https://img.shields.io/badge/language-C-orange)

## 📋 Содержание
- [Обзор проекта](#обзор-проекта)
- [Установка и сборка](#установка-и-сборка)
- [Функциональность по спринтам](#функциональность-по-спринтам)
- [Использование](#использование)
- [Тестирование](#тестирование)
- [Структура проекта](#структура-проекта)
- [Реализованные стандарты](#реализованные-стандарты)


## 🎯 Обзор проекта

CryptoCore представляет собой консольную утилиту, предоставляющую:

| Функция | Описание |
|---------|----------|
| 🔐 **Симметричное шифрование** | AES-128 в 6 режимах |
| 📊 **Хеш-функции** | SHA-256, BLAKE2b |
| 🔏 **Аутентификация** | HMAC-SHA256, AES-GCM |
| 🔑 **Управление ключами** | PBKDF2, иерархия ключей |
| 🎲 **Генерация случайных чисел** | Криптографически безопасный ГСЧ |

Проект разрабатывался поэтапно по 8 спринтам, каждый из которых добавлял новую функциональность.

## 🚀 Установка и сборка

### Требования
- **ОС**: Linux/Unix
- **Компилятор**: GCC 7.0+ или Clang 6.0+
- **Библиотеки**: OpenSSL development libraries
- **Сборка**: GNU Make

### Установка на Ubuntu/Debian
```bash
sudo apt update
sudo apt install build-essential libssl-dev
```
### Сборка проекта
# Клонирование репозитория
```bash
git clone <https://github.com/Crowley-dch/CLI---util.git>
cd CLI---utils
```
```bash
# Сборка утилиты
make

# Запуск тестов
make test
```
## 💻 Использование
### Базовые команды
### Шифрование файла:

```bash
./cryptocore --algorithm aes --mode cbc --encrypt \
  --key 00112233445566778899aabbccddeeff \
  --input документ.txt --output зашифрованный.bin
  ```
### Дешифрование файла:
```bash
./cryptocore --algorithm aes --mode cbc --decrypt \
  --key 00112233445566778899aabbccddeeff \
  --input зашифрованный.bin --output расшифрованный.txt
  ```
### Вычисление хеша:

```bash
./cryptocore dgst --algorithm sha256 --input файл.iso
./cryptocore dgst --algorithm blake2b --input файл.iso --length 32
```
### Генерация HMAC:

```bash
./cryptocore dgst --algorithm sha256 --hmac \
  --key 00112233445566778899aabbccddeeff \
  --input сообщение.txt
```
### Аутентифицированное шифрование (GCM):

```bash
./cryptocore --algorithm aes --mode gcm --encrypt \
  --key 00112233445566778899aabbccddeeff \
  --input данные.bin --output зашифрованные.bin \
  --aad "метаданные_версия_2"
```
### Формирование ключа из пароля:

```bash
./cryptocore derive --password "МойСекретныйПароль" \
  --salt c4f32a1b --iterations 100000 --length 32
  ```
### Автоматическая генерация ключей
Если не указать ключ при шифровании, он будет сгенерирован автоматически:

```bash
./cryptocore --algorithm aes --mode cbc --encrypt \
  --input plaintext.txt --output ciphertext.bin

# Вывод: [INFO] Generated random key: 1a2b3c4d5e6f7890fedcba9876543210
```